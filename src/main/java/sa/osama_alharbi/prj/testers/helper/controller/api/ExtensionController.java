package sa.osama_alharbi.prj.testers.helper.controller.api;

import lombok.extern.slf4j.Slf4j;
import org.apache.commons.io.FileUtils;
import org.apache.commons.io.IOUtils;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.io.File;
import java.io.IOException;

//@RestController
//@RequestMapping("/api/v1/extension")
//@Slf4j
public class ExtensionController {

//    @GetMapping(value = "/zip",produces = MediaType.MULTIPART_FORM_DATA_VALUE)
//    public ResponseEntity<byte[]> downloadExtension() throws IOException {
//        File file = new File("extension/extension.zip");
//        return new ResponseEntity<>(FileUtils.readFileToByteArray(file), HttpStatus.OK);
//    }
}
